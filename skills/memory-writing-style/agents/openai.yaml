interface:
  display_name: "memory-writing-style"
  short_description: "Apply my writing style to the request that follows"
  default_prompt: "Use $memory-writing-style to complete my request in my writing style."

policy:
  allow_implicit_invocation: true
