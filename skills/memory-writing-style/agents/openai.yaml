interface:
  display_name: "memory-writing-style"
  short_description: "Apply the current user's Memory-derived writing style"
  default_prompt: "Use $memory-writing-style to write or rewrite this content in my established style."

policy:
  allow_implicit_invocation: true
